---
skill: bmad-l3io-pm-sprint-execute
phase: complete
classification: complex-workflow
last_touched: 2026-05-15
---

# Decision Log

## Session 2026-05-15 — Initial Build

**Classification: Complex Workflow.** Per-story loop (5 sub-phases) + 8 closure phases = well over 250 lines inline. Carved into two references: `story-loop.md` (per-story) and `sprint-closure.md` (closure). Each file is self-contained for compaction survival.

**No Decision-Log Workspace.** Sprint-execute is a run-once orchestration workflow — it produces closure artifacts to disk but is not itself a revisable artifact. DLW pattern skipped intentionally.

**Headless mode supported.** Called by `bmad-l3io-pm-epic-execute` with story keys + sprint/epic numbers as arguments. Emits structured status line on completion.

**Customizable: yes.** Two user-tunable parallelism settings (`parallel_mode`, `max_parallel_subagents`) lifted to `customize.toml`. All artifact paths resolved from core BMad config — no duplication.

**Config path: `{project-root}/_bmad/config.yaml` (`l3io-pm` section).** Modern BMad standard. Legacy per-module `{project-root}/_bmad/bmm/config.yaml` format not used.

**Clean release review: inline subagent analysis.** No external skill dependency — the subagent performs the YAGNI/scope-adherence check directly. Avoids creating a hard dependency on a `bmad-simplify` skill that may not exist in target repos.

**Light architectural drift: inline subagent analysis.** Same rationale — no external skill wrapper; the subagent performs the 5-dimension drift check directly against the architecture doc.

**Story dependencies:** Handled via `depends_on` field check in story files or status file. Stories without declared dependencies run in parallel batches; dependent stories wait.

**bmad-l3io-sec-agent-redteam detection:** Checks `.claude/commands/` for presence at runtime. Skips gracefully if absent — no hard failure.

**Fix loop max 3 iterations.** After 3, escalate to user with 4 explicit options. This specific limit is BMad institutional knowledge the LLM won't default to.
